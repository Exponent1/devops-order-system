# Example environment variables — copy to .env and edit values (DO NOT COMMIT)
# Values below are illustrative; replace with secure credentials for your environment.

POSTGRES_USER=admin
POSTGRES_PASSWORD=change_me
POSTGRES_DB=orders
# Example DB URL: postgres://user:password@order-db:5432/dbname
DB_URL=postgres://admin:change_me@order-db:5432/orders

BROKER_HOST=rabbitmq
BROKER_PORT=5672
REDIS_HOST=inventory-db

# Default stock for inventory-service (integer)
DEFAULT_STOCK=100

# Optional: override service ports for local development
# ORDER_SERVICE_PORT=5000
# INVENTORY_SERVICE_PORT=5001
